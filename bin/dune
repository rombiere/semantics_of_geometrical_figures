(rule
    (target geo.ml)
    (deps geo.sk)
    (action (run necroml -o %{target} %{deps})))

(executable
    (public_name geo_semantics)
    (name main)
    (libraries necroml camlgpc geo_semantics))

(env
    (dev (flags (:standard -w -8))))